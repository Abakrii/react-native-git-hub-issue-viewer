import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  paginationContainer: {
    display: 'flex',
    flexDirection: 'row',
    padding: 10,
  },
  paginationButton: {
    marginLeft: 5,
  },
});

export default styles;
