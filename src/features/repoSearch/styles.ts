import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  searchIssueView: {
    flex: 1,
    backgroundColor: '#FFF',
  },
  textError: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'red',
  },
});

export default styles;
